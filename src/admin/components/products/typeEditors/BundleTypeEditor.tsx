/**
 * @deprecated Bundle editing moved to BundleEditor (/admin/bundles).
 * Helpers live in @/admin/lib/bundleHelpers.
 */
export {
  getBundleIncludedIds,
  syncBundleIncludes,
  computeBundleTotals,
} from "@/admin/lib/bundleHelpers";
