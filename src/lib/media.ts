export function videoMedia(slug: string) {
  return `/media/videos/${slug}.jpg`;
}

export function characterMedia(slug: string) {
  return `/media/characters/${slug}.jpg`;
}

export function collectionMedia(slug: string) {
  return `/media/collections/${slug}.jpg`;
}

export function categoryMedia(slug: string) {
  return `/media/categories/${slug}.jpg`;
}
